package sorting;

import structures.Queue;

public class Divider<T extends Comparable<T>> {
	Queue<T> input;
	Queue<T> output1;
	Queue<T> output2;

	public Divider(Queue<T> i, Queue<T> o1, Queue<T> o2) {
		input = i;
		output1 = o1;
		output2 = o2;
	}

	public void divi() {
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}

		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() > 1) {
			output1.enqueue(input.dequeue());
			output2.enqueue(input.dequeue());
		}
		if (input.size() == 1) {
			output1.enqueue(input.dequeue());
		}
		if (input.size() >= 1)
			divi();
	}
}
